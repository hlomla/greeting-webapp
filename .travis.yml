language: node_js
node_js:
  - 12

env:
  - DATABASE_URL=postgresql://codex:codex123@localhost:5432/my_database
before_script:
  - psql -c 'create database  my_database;' -U postgres
  - psql -U postgres -d my_database -a -f my_database.sql
services:
  - postgresql